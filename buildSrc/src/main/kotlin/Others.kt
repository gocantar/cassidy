object Others {

    object Versions {
        const val gradleBintray = "1.8.4"
    }

    object Libraries

    object Plugins {
        const val bintray = "com.jfrog.bintray.gradle:gradle-bintray-plugin:${Versions.gradleBintray}"
    }
}